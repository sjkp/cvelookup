---
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';
import { getAllCVES, getAllSlugs } from '../getDataHelper';
import DateTime from '../components/DateTime.astro';
import SeverityBox from '../components/SeverityBox.astro';


const cves = getAllCVES();


// // The getStaticPaths() is required for static Astro sites.
// // If using SSR, you will not need this function.
// export async function getStaticPaths() {
//   const cves = getAllCVES();

//   return cves.map(p => ({
// 	params: {slug: p.slug},
// 	props: {cves: [p]}
//   }));
// }
---

<Layout title="Welcome to Astro. 123">
	<main>

			
	
		</ul>
		<div class="container mx-auto bg-white rounded-lg p-8 shadow-lg">
		<div class="space-y-8 relative before:absolute before:inset-0 before:ml-5 before:-translate-x-px md:before:mx-auto md:before:translate-x-0 before:h-full before:w-0.5 before:bg-gradient-to-b before:from-transparent before:via-slate-300 before:to-transparent">
			{cves.map(s => (        			

				<a class="relative flex items-center justify-between md:justify-normal md:odd:flex-row-reverse group is-active" href={s.slug.split('/').at(-1)}>  
				<div class="flex items-center justify-center w-10 h-10 rounded-full border border-white bg-slate-300 group-[.is-active]:bg-emerald-500 text-slate-500 group-[.is-active]:text-emerald-50 shadow shrink-0 md:order-1 md:group-odd:-translate-x-1/2 md:group-even:translate-x-1/2">
					<svg class="fill-current" xmlns="http://www.w3.org/2000/svg" width="12" height="10">
						<path fill-rule="nonzero" d="M10.422 1.257 4.655 7.025 2.553 4.923A.916.916 0 0 0 1.257 6.22l2.75 2.75a.916.916 0 0 0 1.296 0l6.415-6.416a.916.916 0 0 0-1.296-1.296Z" />
					</svg>
				</div>
			
				<div class="w-[calc(100%-4rem)] md:w-[calc(50%-2.5rem)] bg-white p-4 rounded border border-slate-200 shadow">
					<div class="flex items-center justify-between space-x-2 mb-1">
						<time class="font-bold text-slate-900"><DateTime date={s.published!} /></time>
						<div class="font-caveat font-medium"><SeverityBox severity={s.database_specific?.severity}></SeverityBox></div>
					</div>
					<div class="text-slate-500">{s.summary}</div>
				</div>
			</a>

			))}						
		</div>
	</div>
	</main>
</Layout>

